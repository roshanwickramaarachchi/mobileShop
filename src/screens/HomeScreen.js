import React from 'react';
import {View, StyleSheet, Text} from 'react-native';

const HomeScreen = () => {
  return <Text style={{fontSize: 48}}>HomeScreen</Text>;
};

const styles = StyleSheet.create({});

export default HomeScreen;